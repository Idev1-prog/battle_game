#include <iostream>
#include "GameElements.h"
#include <locale.h>
int main() {
    setlocale(LC_ALL, "Rus");
    Game game;
    game.start();

    system("pause");
    system("pause");
    system("pause");
    system("pause");
    return 0;
}